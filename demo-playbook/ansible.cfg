[defaults]
# Use the standard roles path
roles_path = ../ansible/roles

# Disable host key checking for demo purposes (enable in production)
host_key_checking = False

# Improve output formatting
force_color = True
