name: kokoro

services:
  kokoro-tts:
    build:
      context: ./kokoro-fastapi
      dockerfile: docker/cpu/Dockerfile
    volumes:
      - ./models:/app/api/src/models
    environment:
      - DOWNLOAD_MODEL=true
    restart: unless-stopped

  kokoro-proxy:
    image: nginx:alpine
    ports:
      - "8443:443"
    volumes:
      - ./certs/localhost.pem:/etc/nginx/certs/cert.pem:ro
      - ./certs/localhost-key.pem:/etc/nginx/certs/key.pem:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - kokoro-tts
    restart: unless-stopped
