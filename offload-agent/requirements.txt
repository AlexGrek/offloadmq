# Core dependencies
requests>=2.25.0
psutil>=5.8.0
websocket-client>=1.6.0
PyYAML>=6.0

# Web UI
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# PDF rendering (for LLM vision tasks)
pymupdf>=1.24.0

# GPU detection (optional but recommended)
boto3
GPUtil>=1.4.0
pydantic
pynvml>=11.4.0
typer[all]
colorlog

# ONNX inference (included in default agent build)
onnxruntime>=1.17.0
numpy>=1.24.0

# Windows tray icon (only needed for Windows .exe build)
pystray>=0.19.0
Pillow>=10.0.0

# Note: pynvml is for NVIDIA GPU detection
# GPUtil provides a more general GPU interface
# Both are optional - the script will work without them but won't detect GPU info

# Type checking (build-time only)
mypy>=1.9.0
types-requests>=2.31.0
types-PyYAML>=6.0.1